import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { CalendarDays, ClipboardClock, FileDown, Pencil, UserRound, UsersRound } from 'lucide-react-native';
import { useState } from 'react';
import { Alert, Pressable, RefreshControl, ScrollView, StyleSheet, Text, View } from 'react-native';
import Animated, { FadeInUp } from 'react-native-reanimated';
import { AppHeader, SectionTitle } from '@/components/ui/app-header';
import { FormError, FormField, SelectChips, SubmitButton } from '@/components/ui/form-controls';
import { GlassCard } from '@/components/ui/glass-card';
import { ModalSheet } from '@/components/ui/modal-sheet';
import { ScreenBackground } from '@/components/ui/screen-background';
import { Colors, Fonts, Radius, Spacing } from '@/constants/theme';
import { Driver, HistoryAssignment, MemberOption, mobileAction, TerritoryRange } from '@/lib/api';
import { saveAndSharePdf } from '@/lib/pdf';
import { useAuth } from '@/providers/auth-provider';

type HistoryFilter = 'TODAS' | 'CONDUCTOR' | 'PERSONAL';
const MONTH_NAMES = ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio', 'Julio', 'Agosto', 'Septiembre', 'Octubre', 'Noviembre', 'Diciembre'];

function toInputDate(value?: string | null) { return value ? new Date(value).toISOString().slice(0, 10) : ''; }
function parseMonthKey(value: string) { const [year, month] = value.split('-').map(Number); return { year, month }; }
function formatMonthKey(date: Date) { return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, '0')}`; }

export default function HistoryScreen() {
  const { token } = useAuth();
  const queryClient = useQueryClient();
  const now = new Date();
  const currentMonth = formatMonthKey(now);
  const [filter, setFilter] = useState<HistoryFilter>('TODAS');
  const [modal, setModal] = useState(false);
  const [editing, setEditing] = useState<HistoryAssignment | null>(null);
  const [assigneeId, setAssigneeId] = useState<string>();
  const [assignedDate, setAssignedDate] = useState('');
  const [returnedDate, setReturnedDate] = useState('');
  const [error, setError] = useState('');
  const [pdfModal, setPdfModal] = useState(false);
  const [fromKey, setFromKey] = useState(currentMonth);
  const [toKey, setToKey] = useState(currentMonth);
  const [selectedRanges, setSelectedRanges] = useState<string[]>([]);
  const [includeActive, setIncludeActive] = useState(false);
  const [pdfError, setPdfError] = useState('');
  const [pdfLoading, setPdfLoading] = useState(false);
  const historyQuery = useQuery({ queryKey: ['history'], queryFn: () => mobileAction<HistoryAssignment[]>('getUnifiedHistory', { page: 1, pageSize: 100 }, token), enabled: Boolean(token) });
  const rangesQuery = useQuery({ queryKey: ['territory-ranges'], queryFn: () => mobileAction<TerritoryRange[]>('getAvailableTerritoryRanges', {}, token), enabled: Boolean(token) });
  const driversQuery = useQuery({ queryKey: ['driver-select'], queryFn: () => mobileAction<Driver[]>('getAllDriversForSelect', {}, token), enabled: Boolean(token) });
  const membersQuery = useQuery({ queryKey: ['member-select'], queryFn: () => mobileAction<MemberOption[]>('getAllMembersForSelect', {}, token), enabled: Boolean(token) });
  const mutation = useMutation({ mutationFn: ({ action, params }: { action: string; params: Record<string, unknown> }) => mobileAction(action, params, token), onSuccess: async () => { await queryClient.invalidateQueries({ queryKey: ['history'] }); await queryClient.invalidateQueries({ queryKey: ['territories'] }); closeModal(); }, onError: (value: Error) => setError(value.message) });
  const histories = (historyQuery.data || []).filter((item) => filter === 'TODAS' || item.type === filter);
  const options = editing?.type === 'CONDUCTOR' ? (driversQuery.data || []).map((driver) => ({ value: driver.id, label: driver.name, detail: driver.group.name })) : (membersQuery.data || []).map((member) => ({ value: member.id, label: member.name, detail: member.group.name }));

  function openEdit(item: HistoryAssignment) { setEditing(item); setAssigneeId(item.assigneeId); setAssignedDate(toInputDate(item.assignedDate)); setReturnedDate(toInputDate(item.endDate)); setError(''); setModal(true); }
  function closeModal() { setModal(false); setEditing(null); setError(''); }
  function save() { if (!editing || !assigneeId || !assignedDate || !returnedDate) { setError('Completá la persona y ambas fechas.'); return; } const start = new Date(`${assignedDate}T12:00:00`); const end = new Date(`${returnedDate}T12:00:00`); if (Number.isNaN(start.getTime()) || Number.isNaN(end.getTime()) || end < start) { setError('Revisá el formato de fechas YYYY-MM-DD.'); return; } const params = editing.type === 'CONDUCTOR' ? { assignmentId: editing.id, data: { driverId: assigneeId, startDate: start.toISOString(), endDate: end.toISOString() } } : { assignmentId: editing.id, data: { memberId: assigneeId, assignedDate: start.toISOString(), returnedDate: end.toISOString() } }; mutation.mutate({ action: editing.type === 'CONDUCTOR' ? 'updateAssignment' : 'updatePersonalAssignment', params }); }
  function openPdfModal() { setFromKey(currentMonth); setToKey(currentMonth); setSelectedRanges([]); setIncludeActive(false); setPdfError(''); setPdfModal(true); }
  function closePdfModal() { if (!pdfLoading) setPdfModal(false); }
  function toggleRange(label: string) { setSelectedRanges((current) => current.includes(label) ? current.filter((item) => item !== label) : [...current, label]); }
  async function generatePdfs() {
    const from = parseMonthKey(fromKey); const to = parseMonthKey(toKey);
    if (!Number.isInteger(from.year) || !Number.isInteger(from.month) || !Number.isInteger(to.year) || !Number.isInteger(to.month) || from.month < 1 || from.month > 12 || to.month < 1 || to.month > 12) { setPdfError('Usá el formato YYYY-MM para el período.'); return; }
    if (fromKey > toKey) { setPdfError('El período Desde no puede ser posterior a Hasta.'); return; }
    if (!selectedRanges.length) { setPdfError('Seleccioná al menos un rango de territorios.'); return; }
    setPdfLoading(true); setPdfError('');
    try {
      for (const label of selectedRanges) {
        const [startNumber, endNumber] = label.split('-').map(Number);
        const pdf = await mobileAction<Blob>('exportTerritoryHistoryPdf', { startNumber, endNumber, fromMonth: from.month, fromYear: from.year, toMonth: to.month, toYear: to.year, includeActive }, token);
        const fromLabel = `${MONTH_NAMES[from.month - 1]}-${from.year}`;
        const toLabel = `${MONTH_NAMES[to.month - 1]}-${to.year}`;
        const period = fromKey === toKey ? fromLabel : `${fromLabel}_${toLabel}`;
        await saveAndSharePdf(pdf, `S-13-S_Territorios_${label}_${period}.pdf`);
      }
      setPdfModal(false);
      Alert.alert('PDF generado', `${selectedRanges.length} formulario(s) S-13-S listo(s) para descargar o compartir.`);
    } catch (value) { setPdfError(value instanceof Error ? value.message : 'No se pudo generar el PDF.'); } finally { setPdfLoading(false); }
  }

  return <ScreenBackground><ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false} refreshControl={<RefreshControl refreshing={historyQuery.isRefetching} onRefresh={() => historyQuery.refetch()} tintColor={Colors.mint} colors={[Colors.mint]} />}><AppHeader /><View style={styles.intro}><Text style={styles.eyebrow}>CONTROL ADMINISTRATIVO</Text><Text style={styles.title}>Historial</Text><Text style={styles.subtitle}>Consultá los territorios devueltos y corregí fechas o responsables cuando sea necesario.</Text></View><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.filters}>{(['TODAS', 'CONDUCTOR', 'PERSONAL'] as HistoryFilter[]).map((item) => <Pressable key={item} onPress={() => setFilter(item)} style={[styles.filter, filter === item && styles.filterActive]}><Text style={[styles.filterText, filter === item && styles.filterTextActive]}>{item === 'TODAS' ? 'Todos' : item === 'CONDUCTOR' ? 'Por grupo' : 'Personales'}</Text></Pressable>)}</ScrollView><View style={styles.actionRow}><SectionTitle eyebrow="REGISTRO" title={`${histories.length} devoluciones`} /><Pressable onPress={openPdfModal} style={styles.pdfButton}><FileDown size={15} color={Colors.ink} /><Text style={styles.pdfButtonText}>PDF S-13-S</Text></Pressable></View>{histories.length ? <View style={styles.list}>{histories.map((item, index) => <Animated.View key={`${item.type}-${item.id}`} entering={FadeInUp.duration(420).delay(Math.min(index, 6) * 50)}><HistoryCard item={item} onEdit={() => openEdit(item)} /></Animated.View>)}</View> : <GlassCard style={styles.empty}><ClipboardClock size={28} color={Colors.textDim} /><Text style={styles.emptyTitle}>Todavía no hay historial</Text><Text style={styles.emptyCopy}>Las asignaciones devueltas aparecerán en esta vista.</Text></GlassCard>}</ScrollView><ModalSheet visible={modal} onClose={closeModal} eyebrow="EDITAR HISTORIAL" title="Corregir devolución"><SelectChips label={editing?.type === 'CONDUCTOR' ? 'Conductor' : 'Integrante'} value={assigneeId} options={options} onChange={setAssigneeId} /><FormField label="Fecha de asignación" value={assignedDate} onChangeText={setAssignedDate} placeholder="YYYY-MM-DD" autoCapitalize="none" /><FormField label="Fecha de devolución" value={returnedDate} onChangeText={setReturnedDate} placeholder="YYYY-MM-DD" autoCapitalize="none" /><Text style={styles.formHint}>Los cambios se aplican al registro histórico ya devuelto.</Text><FormError message={error} /><SubmitButton label="Guardar corrección" loading={mutation.isPending} onPress={save} /></ModalSheet><ModalSheet visible={pdfModal} onClose={closePdfModal} eyebrow="FORMULARIO OFICIAL" title="Exportar a PDF S-13-S"><FormField label="Desde (YYYY-MM)" value={fromKey} onChangeText={setFromKey} placeholder="2026-01" autoCapitalize="none" /><FormField label="Hasta (YYYY-MM)" value={toKey} onChangeText={setToKey} placeholder="2026-01" autoCapitalize="none" /><View style={styles.rangeHeader}><Text style={styles.formLabel}>Rangos de territorios</Text><Pressable onPress={() => setSelectedRanges(selectedRanges.length === (rangesQuery.data || []).length ? [] : (rangesQuery.data || []).map((range) => range.label))}><Text style={styles.selectAll}>{selectedRanges.length === (rangesQuery.data || []).length ? 'Quitar todos' : 'Seleccionar todos'}</Text></Pressable></View><View style={styles.rangeList}>{(rangesQuery.data || []).map((range) => <Pressable key={range.label} onPress={() => toggleRange(range.label)} style={[styles.rangeRow, selectedRanges.includes(range.label) && styles.rangeRowActive]}><View><Text style={styles.rangeLabel}>Territorios {range.label}</Text><Text style={styles.rangeCount}>{range.count} {range.count === 1 ? 'territorio' : 'territorios'}</Text></View><View style={[styles.check, selectedRanges.includes(range.label) && styles.checkActive]}>{selectedRanges.includes(range.label) && <Text style={styles.checkText}>✓</Text>}</View></Pressable>)}</View><Pressable onPress={() => setIncludeActive((current) => !current)} style={[styles.activeOption, includeActive && styles.activeOptionSelected]}><View><Text style={styles.activeOptionTitle}>Incluir asignaciones activas</Text><Text style={styles.activeOptionCopy}>Agrega registros que todavía están en curso.</Text></View><View style={[styles.check, includeActive && styles.checkActive]}>{includeActive && <Text style={styles.checkText}>✓</Text>}</View></Pressable><FormError message={pdfError} /><SubmitButton label={pdfLoading ? 'Generando...' : `Generar ${selectedRanges.length || ''} PDF`} loading={pdfLoading} disabled={rangesQuery.isLoading} onPress={generatePdfs} /></ModalSheet></ScreenBackground>;
}

function HistoryCard({ item, onEdit }: { item: HistoryAssignment; onEdit: () => void }) { const personal = item.type === 'PERSONAL'; return <GlassCard style={styles.card}><View style={styles.cardTop}><View style={[styles.iconBox, personal ? styles.personalIcon : styles.groupIcon]}>{personal ? <UserRound size={18} color={Colors.mint} /> : <UsersRound size={18} color={Colors.blueBright} />}</View><View style={styles.cardMain}><Text style={styles.cardTitle}>Territorio {item.territoryNumber}</Text><Text style={styles.cardMeta}>{item.assigneeName} · {item.groupName}</Text></View><Pressable onPress={onEdit} style={styles.iconButton}><Pencil size={15} color={Colors.textMuted} /></Pressable></View><View style={styles.dates}><View><Text style={styles.smallLabel}>ASIGNADO</Text><Text style={styles.date}>{new Date(item.assignedDate).toLocaleDateString('es-AR')}</Text></View><CalendarDays size={15} color={Colors.textDim} /><View><Text style={styles.smallLabel}>DEVUELTO</Text><Text style={styles.date}>{item.endDate ? new Date(item.endDate).toLocaleDateString('es-AR') : 'Sin fecha'}</Text></View></View></GlassCard>; }

const styles = StyleSheet.create({ content: { paddingBottom: Spacing.eight, gap: Spacing.four }, intro: { paddingHorizontal: Spacing.four, gap: 5 }, eyebrow: { color: Colors.mint, fontFamily: Fonts.rounded, fontSize: 10, letterSpacing: 1.8 }, title: { color: Colors.text, fontFamily: Fonts.rounded, fontSize: 32, letterSpacing: -0.8 }, subtitle: { color: Colors.textMuted, fontFamily: Fonts.sans, fontSize: 14, lineHeight: 21 }, filters: { gap: 8, paddingHorizontal: Spacing.four }, filter: { paddingHorizontal: 15, paddingVertical: 9, borderRadius: Radius.pill, borderWidth: 1, borderColor: Colors.border, backgroundColor: 'rgba(168,183,204,0.05)' }, filterActive: { backgroundColor: Colors.mint, borderColor: Colors.mint }, filterText: { color: Colors.textDim, fontFamily: Fonts.rounded, fontSize: 11 }, filterTextActive: { color: Colors.ink }, actionRow: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: Spacing.four }, pdfButton: { flexDirection: 'row', alignItems: 'center', gap: 6, paddingHorizontal: 11, paddingVertical: 9, borderRadius: Radius.pill, backgroundColor: Colors.mint }, pdfButtonText: { color: Colors.ink, fontFamily: Fonts.rounded, fontSize: 10 }, list: { gap: Spacing.three, paddingHorizontal: Spacing.four }, card: { padding: Spacing.three, gap: Spacing.three }, cardTop: { flexDirection: 'row', alignItems: 'center', gap: 11 }, iconBox: { width: 42, height: 42, alignItems: 'center', justifyContent: 'center', borderRadius: 14 }, personalIcon: { backgroundColor: 'rgba(94,234,212,0.13)' }, groupIcon: { backgroundColor: 'rgba(51,133,246,0.14)' }, cardMain: { flex: 1, gap: 4 }, cardTitle: { color: Colors.text, fontFamily: Fonts.rounded, fontSize: 15 }, cardMeta: { color: Colors.textDim, fontFamily: Fonts.sans, fontSize: 12 }, iconButton: { width: 32, height: 32, alignItems: 'center', justifyContent: 'center', borderRadius: Radius.pill, backgroundColor: 'rgba(168,183,204,0.08)' }, dates: { flexDirection: 'row', alignItems: 'center', gap: 14 }, smallLabel: { color: Colors.textDim, fontFamily: Fonts.rounded, fontSize: 8, letterSpacing: 1 }, date: { color: Colors.textMuted, fontFamily: Fonts.sans, fontSize: 12, marginTop: 4 }, formLabel: { color: Colors.textMuted, fontFamily: Fonts.rounded, fontSize: 10, letterSpacing: 1.1, textTransform: 'uppercase' }, rangeHeader: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' }, selectAll: { color: Colors.blueBright, fontFamily: Fonts.rounded, fontSize: 10 }, rangeList: { gap: 8 }, rangeRow: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', padding: 12, borderRadius: Radius.medium, borderWidth: 1, borderColor: Colors.border, backgroundColor: 'rgba(168,183,204,0.06)' }, rangeRowActive: { borderColor: Colors.mint, backgroundColor: 'rgba(94,234,212,0.12)' }, rangeLabel: { color: Colors.text, fontFamily: Fonts.rounded, fontSize: 12 }, rangeCount: { color: Colors.textDim, fontFamily: Fonts.sans, fontSize: 10, marginTop: 3 }, check: { width: 22, height: 22, alignItems: 'center', justifyContent: 'center', borderRadius: 7, borderWidth: 1, borderColor: Colors.border, backgroundColor: 'rgba(6,18,37,0.42)' }, checkActive: { borderColor: Colors.mint, backgroundColor: Colors.mint }, checkText: { color: Colors.ink, fontFamily: Fonts.rounded, fontSize: 14 }, activeOption: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', gap: 12, padding: 12, borderRadius: Radius.medium, borderWidth: 1, borderColor: Colors.border, backgroundColor: 'rgba(168,183,204,0.06)' }, activeOptionSelected: { borderColor: Colors.warning, backgroundColor: 'rgba(245,201,106,0.11)' }, activeOptionTitle: { color: Colors.text, fontFamily: Fonts.rounded, fontSize: 12 }, activeOptionCopy: { color: Colors.textDim, fontFamily: Fonts.sans, fontSize: 10, marginTop: 3 }, formHint: { color: Colors.textDim, fontFamily: Fonts.sans, fontSize: 12, lineHeight: 18 }, empty: { marginHorizontal: Spacing.four, alignItems: 'center', padding: Spacing.five, gap: 8 }, emptyTitle: { color: Colors.text, fontFamily: Fonts.rounded, fontSize: 15 }, emptyCopy: { color: Colors.textDim, fontFamily: Fonts.sans, fontSize: 12, textAlign: 'center' } });
